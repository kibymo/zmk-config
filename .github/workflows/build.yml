name: Build ZMK Firmware

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3.0
    with:
      # 預設 build_matrix_path = "build.yaml"，可省略
      # build_matrix_path: build.yaml
      config_path: config
