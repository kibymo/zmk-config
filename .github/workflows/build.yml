    - name: ZMK Docker Build
      uses: docker://zmkfirmware/zephyr-west-action:latest
      with:
        args: 'init "-l ."'
    
    - name: West Update
      uses: docker://zmkfirmware/zephyr-west-action:latest
      with:
        args: 'update'
